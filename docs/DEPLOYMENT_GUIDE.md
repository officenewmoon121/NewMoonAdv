# 🚀 دليل رفع الموقع - New Moon V1

**التاريخ:** 2025-01-27  
**الحالة:** Phase 0 - جاهز للرفع

---

## 📋 قبل الرفع

### ✅ Checklist

- [x] جميع الصفحات تعمل محلياً
- [x] جميع الروابط تعمل
- [x] لا توجد أخطاء حرجة
- [x] الموقع جاهز للرفع

---

## 🎯 طرق الرفع

### الطريقة 1: FTP/SFTP (الأكثر شيوعاً)

#### الأدوات المطلوبة:

- **FileZilla** (مجاني) - [تحميل](https://filezilla-project.org/)
- **WinSCP** (مجاني) - [تحميل](https://winscp.net/)
- أو أي FTP Client آخر

#### خطوات الرفع:

1. **فتح FileZilla/WinSCP**

2. **إدخال بيانات Server:**

   ```
   Host: [عنوان الـ Server]
   Username: [اسم المستخدم]
   Password: [كلمة المرور]
   Port: 21 (FTP) أو 22 (SFTP)
   ```

3. **الاتصال بالـ Server**

4. **رفع الملفات:**

   - في الجانب الأيسر: مجلد المشروع المحلي
   - في الجانب الأيمن: مجلد `public_html` أو `www` على Server
   - سحب جميع الملفات من اليسار لليمين

5. **التحقق من الرفع:**
   - زيارة الموقع في المتصفح
   - اختبار جميع الصفحات

---

### الطريقة 2: cPanel File Manager

#### خطوات الرفع:

1. **تسجيل الدخول لـ cPanel**

2. **فتح File Manager**

3. **الانتقال لمجلد `public_html`**

4. **رفع الملفات:**

   - Upload → Select Files
   - اختيار جميع ملفات المشروع
   - انتظار اكتمال الرفع

5. **استخراج الملفات (إذا كانت مضغوطة):**
   - Extract → اختيار الملف المضغوط

---

### الطريقة 3: Git (للمطورين)

#### خطوات الرفع:

1. **إنشاء Repository على GitHub/GitLab**

2. **ربط المشروع:**

   ```bash
   git init
   git add .
   git commit -m "Phase 0: قفل V1"
   git remote add origin [رابط Repository]
   git push -u origin main
   ```

3. **ربط Server بـ Git:**
   - استخدام Git Pull على Server
   - أو استخدام CI/CD (GitHub Actions, GitLab CI)

---

## 📁 الملفات المطلوب رفعها

### ✅ يجب رفعها:

```
NewMoon_V1_ProductReady/
├── index.html
├── about.html
├── contact.html
├── solutions.html
├── portfolio.html
├── companies.html
├── studio.html
├── return-policy.html
├── feedback-admin.html
├── components/          (مجلد كامل)
├── css/                 (مجلد كامل)
├── js/                  (مجلد كامل)
├── data/                (مجلد كامل)
├── assets/              (مجلد كامل)
├── solutions/           (مجلد كامل)
└── docs/                (اختياري - للتوثيق فقط)
```

### ❌ لا ترفع:

- `.vscode/` (إعدادات Editor)
- `.cursor/` (إعدادات Cursor)
- `.git/` (إذا كان موجود)
- `Backups/` (إذا كان موجود)
- ملفات `.gitignore`, `.gitattributes`

---

## ⚙️ إعدادات Server

### 1. ملف `.htaccess` (لـ Apache)

أنشئ ملف `.htaccess` في المجلد الرئيسي:

```apache
# Enable Rewrite Engine
RewriteEngine On

# Force HTTPS (اختياري)
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Remove .html extension
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.*)\.html$ /$1 [L,R=301]

# Default page
DirectoryIndex index.html

# Error pages (اختياري)
ErrorDocument 404 /index.html
```

### 2. ملف `web.config` (لـ IIS/Windows Server)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>
        <defaultDocument>
            <files>
                <clear />
                <add value="index.html" />
            </files>
        </defaultDocument>
        <httpErrors errorMode="Custom" existingResponse="Replace">
            <remove statusCode="404" />
            <error statusCode="404" responseMode="ExecuteURL" path="/index.html" />
        </httpErrors>
    </system.webServer>
</configuration>
```

---

## 🔍 بعد الرفع - الاختبار

### 1. اختبار الصفحات الرئيسية:

- [ ] `https://yourdomain.com/` - الصفحة الرئيسية
- [ ] `https://yourdomain.com/about.html` - من نحن
- [ ] `https://yourdomain.com/contact.html` - التواصل
- [ ] `https://yourdomain.com/solutions.html` - الحلول
- [ ] `https://yourdomain.com/portfolio.html` - الأعمال
- [ ] `https://yourdomain.com/companies.html` - العملاء
- [ ] `https://yourdomain.com/studio.html` - الاستوديو

### 2. اختبار الوظائف:

- [ ] القائمة تعمل (Mobile + Desktop)
- [ ] Feedback System يعمل
- [ ] Companies "مشاهدة المزيد" يعمل
- [ ] Portfolio "مشاهدة المزيد" يعمل
- [ ] جميع الروابط تعمل

### 3. اختبار الأداء:

- [ ] سرعة التحميل جيدة
- [ ] الصور تظهر بشكل صحيح
- [ ] لا توجد أخطاء في Console (F12)

---

## 🛠️ حل المشاكل الشائعة

### المشكلة 1: الصور لا تظهر

**الحل:**

- تأكد من رفع مجلد `assets/` بالكامل
- تحقق من مسارات الصور (relative paths)

### المشكلة 2: CSS/JS لا يعمل

**الحل:**

- تأكد من رفع مجلدات `css/` و `js/` بالكامل
- تحقق من مسارات الملفات

### المشكلة 3: 404 Error للصفحات

**الحل:**

- تأكد من رفع جميع ملفات `.html`
- تحقق من إعدادات Server (`.htaccess` أو `web.config`)

### المشكلة 4: القائمة لا تعمل

**الحل:**

- تأكد من رفع `components/header.html`
- تحقق من رفع `js/components/navigation.js`
- افتح Console (F12) للتحقق من الأخطاء

---

## 📝 ملاحظات مهمة

1. **احفظ نسخة احتياطية قبل الرفع**
2. **اختبر الموقع محلياً قبل الرفع**
3. **استخدم HTTPS (موصى به)**
4. **اختبر على أجهزة مختلفة بعد الرفع**

---

## 🔐 الأمان

### بعد الرفع:

- [ ] تفعيل HTTPS/SSL
- [ ] إخفاء `feedback-admin.html` (إضافة حماية)
- [ ] تحديث كلمات المرور
- [ ] إزالة ملفات التوثيق من Server (docs/)

---

## 📞 الدعم

إذا واجهت مشاكل:

1. راجع `docs/PHASE_0_COMPLETE.md`
2. راجع `docs/HOW_TO_RESTORE.md`
3. تحقق من Console (F12) للأخطاء
4. راجع ملفات Server Logs

---

**آخر تحديث:** 2025-01-27
