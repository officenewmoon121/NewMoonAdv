# 👀 كيف ترى الاقتراحات والشكاوى

**دليل سريع لعرض الرسائل**

---

## 🚀 الطريقة السريعة (الأسهل)

### افتح في المتصفح:
```
feedback-admin.html
```

**ستجد:**
- ✅ جميع الرسائل المرسلة
- ✅ إحصائيات (إجمالي، مقترحات، شكاوى)
- ✅ زر "تصدير JSON" لتحميل جميع الرسائل
- ✅ زر "مسح الكل" لحذف جميع الرسائل

---

## 📊 ماذا ترى في الصفحة؟

### 1. إحصائيات:
- إجمالي الاقتراحات
- عدد المقترحات
- عدد الشكاوى
- عدد المشاكل التقنية

### 2. قائمة الرسائل:
كل رسالة تحتوي على:
- نوع الرسالة (مقترح، شكوى، مشكلة تقنية)
- التاريخ والوقت
- الرسالة
- الاسم (إن وجد)
- البريد الإلكتروني (إن وجد)
- الصفحة التي أُرسلت منها

---

## 💾 تصدير الرسائل

### الطريقة 1: من feedback-admin.html
1. افتح `feedback-admin.html`
2. اضغط "تصدير JSON"
3. سيتم تحميل ملف JSON

### الطريقة 2: من Console
افتح Developer Tools (F12) ثم:
```javascript
// عرض جميع الرسائل
const feedbacks = FeedbackSystem.getAllFeedbacks();
console.log(feedbacks);

// تصدير JSON
const json = JSON.stringify(feedbacks, null, 2);
console.log(json);
```

---

## 📍 أين تُحفظ الرسائل؟

### حالياً:
- **الموقع:** LocalStorage في المتصفح
- **المفتاح:** `newmoon_feedback`
- **الحد الأقصى:** 100 رسالة

### للوصول:
```javascript
// من Console
localStorage.getItem('newmoon_feedback')
```

---

## 🔄 ربط مع Backend (للمستقبل)

### الخطوات:
1. أنشئ API endpoint (PHP/Node.js)
2. غيّر `API_ENDPOINT` في `js/components/feedback.js` (السطر 287)
3. اختبر الإرسال

### مثال:
```javascript
// في js/components/feedback.js
const API_ENDPOINT = 'https://yourdomain.com/api/feedback';
```

---

## ⚠️ ملاحظات

1. **LocalStorage محدود:**
   - البيانات في المتصفح فقط
   - يمكن مسحها بمسح بيانات المتصفح
   - كل مستخدم يرى رسائله فقط

2. **للإنتاج:**
   - يجب ربط النظام مع Backend
   - حفظ في قاعدة بيانات
   - إشعارات Email

---

## 📝 الخلاصة

**لرؤية الرسائل الآن:**
```
افتح: feedback-admin.html
```

**لتصدير الرسائل:**
```
اضغط: "تصدير JSON" في feedback-admin.html
```

**لربط مع Backend:**
```
راجع: docs/FEEDBACK_SYSTEM_GUIDE.md
```

---

**آخر تحديث:** 2025

