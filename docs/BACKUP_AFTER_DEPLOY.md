# 💾 حفظ نسخة احتياطية بعد الرفع

**التاريخ:** 2025-01-27  
**الهدف:** حفظ نسخة آمنة من الموقع بعد رفعه على Server

---

## 📋 خطوات حفظ النسخة الاحتياطية

### الطريقة 1: نسخ يدوي (الأبسط)

#### خطوة 1: إنشاء مجلد النسخ الاحتياطي

```bash
# إنشاء مجلد للنسخ الاحتياطية
mkdir "Z:\Backups\NewMoon_V1"

# نسخ المجلد بالكامل مع التاريخ
xcopy "Z:\NewMoon_V1_ProductReady" "Z:\Backups\NewMoon_V1\Phase0_2025-01-27" /E /I /H /Y
```

#### خطوة 2: ضغط النسخة الاحتياطية (اختياري)

```bash
# استخدام WinRAR أو 7-Zip لضغط المجلد
# أو استخدام PowerShell:
Compress-Archive -Path "Z:\Backups\NewMoon_V1\Phase0_2025-01-27" -DestinationPath "Z:\Backups\NewMoon_V1\Phase0_2025-01-27.zip"
```

---

### الطريقة 2: استخدام Git (موصى بها)

#### خطوة 1: إنشاء Git Repository

```bash
# الانتقال لمجلد المشروع
cd Z:\NewMoon_V1_ProductReady

# إنشاء Git Repository
git init

# إضافة جميع الملفات
git add .

# عمل Commit
git commit -m "Phase 0: قفل V1 - جاهز للرفع - 2025-01-27"

# إنشاء Tag لهذه الحالة
git tag -a v1.0-phase0 -m "Phase 0 Complete - Ready for Launch"
```

#### خطوة 2: رفع على GitHub/GitLab (اختياري)

```bash
# إنشاء Repository على GitHub/GitLab
# ثم ربطه:

git remote add origin https://github.com/yourusername/newmoon-v1.git
git branch -M main
git push -u origin main

# رفع الـ Tags
git push origin v1.0-phase0
```

---

### الطريقة 3: نسخ من Server بعد الرفع

#### خطوة 1: تحميل الملفات من Server

```bash
# استخدام FTP/SFTP لتحميل الملفات من Server
# أو استخدام Git إذا رفعت المشروع على GitHub/GitLab
```

#### خطوة 2: حفظ النسخة المحلية

```bash
# نسخ الملفات المحملة من Server
# إلى مجلد النسخ الاحتياطي
```

---

## 📁 هيكل النسخة الاحتياطية المقترح

```
Z:\Backups\NewMoon_V1\
├── Phase0_2025-01-27\          # النسخة الحالية (Phase 0)
│   └── [جميع ملفات المشروع]
├── Phase0_2025-01-27.zip       # نسخة مضغوطة (اختياري)
└── README.txt                  # ملاحظات عن النسخ الاحتياطية
```

---

## 📝 ملف README للنسخ الاحتياطية

أنشئ ملف `Z:\Backups\NewMoon_V1\README.txt`:

```
New Moon V1 - النسخ الاحتياطية
==============================

Phase0_2025-01-27:
- الحالة: جاهز للرفع
- التاريخ: 2025-01-27
- الوصف: Phase 0 مكتمل - جميع الأخطاء الحرجة تم إصلاحها
- الملفات: جميع ملفات المشروع

للرجوع لهذه النسخة:
1. نسخ المجلد Phase0_2025-01-27
2. استبدال المجلد الحالي
3. أو استخدام Git: git checkout v1.0-phase0
```

---

## ✅ Checklist بعد الرفع

- [ ] رفع الموقع على Server
- [ ] اختبار الموقع على Server
- [ ] عمل نسخة احتياطية محلية
- [ ] حفظ النسخة في مكان آمن
- [ ] توثيق النسخة (تاريخ + وصف)
- [ ] (اختياري) رفع على GitHub/GitLab

---

## 🔄 كيفية استخدام النسخة الاحتياطية

### للرجوع للنسخة:

```bash
# حذف المجلد الحالي (بعد التأكد من النسخ الاحتياطي!)
# ثم نسخ النسخة الاحتياطية
xcopy "Z:\Backups\NewMoon_V1\Phase0_2025-01-27" "Z:\NewMoon_V1_ProductReady" /E /I /H /Y
```

### أو استخدام Git:

```bash
git checkout v1.0-phase0
```

---

## 📌 نصائح مهمة

1. **احفظ نسخة احتياطية قبل أي تعديل كبير**
2. **احفظ نسخة بعد كل Phase مكتمل**
3. **استخدم Git للنسخ الاحتياطية طويلة المدى**
4. **احفظ النسخ في مكان آمن (Cloud + Local)**

---

## 🗂️ أماكن حفظ النسخ الاحتياطية

### محلي:
- `Z:\Backups\NewMoon_V1\`
- External Hard Drive
- USB Drive

### سحابي:
- GitHub/GitLab (موصى به)
- Google Drive
- OneDrive
- Dropbox

---

**آخر تحديث:** 2025-01-27

