# 📨 دليل نظام الاقتراحات والشكاوى

**كيف يعمل النظام وأين تُحفظ الرسائل**

---

## 📍 أين تُحفظ الرسائل حالياً؟

### 1. **LocalStorage (المتصفح)**
الرسائل تُحفظ حالياً في **LocalStorage** في المتصفح تحت المفتاح:
```
newmoon_feedback
```

**الموقع:** في متصفح المستخدم (Chrome, Firefox, etc.)

**المميزات:**
- ✅ سريع ولا يحتاج Backend
- ✅ يعمل بدون إنترنت
- ✅ جاهز للاستخدام فوراً

**العيوب:**
- ⚠️ البيانات في المتصفح فقط (لا يمكنك رؤيتها من مكان آخر)
- ⚠️ يمكن مسحها بمسح بيانات المتصفح
- ⚠️ كل مستخدم يرى رسائله فقط

---

## 👀 كيف ترى الرسائل؟

### الطريقة 1: صفحة Admin (الأسهل)

افتح في المتصفح:
```
feedback-admin.html
```

**المميزات:**
- ✅ عرض جميع الرسائل
- ✅ إحصائيات (إجمالي، مقترحات، شكاوى)
- ✅ تصدير JSON
- ✅ مسح الكل

---

### الطريقة 2: من Console المتصفح

افتح Developer Tools (F12) ثم اكتب:

```javascript
// عرض جميع الرسائل
FeedbackSystem.getAllFeedbacks()

// أو مباشرة من LocalStorage
JSON.parse(localStorage.getItem('newmoon_feedback'))
```

---

### الطريقة 3: تصدير JSON

1. افتح `feedback-admin.html`
2. اضغط "تصدير JSON"
3. سيتم تحميل ملف JSON يحتوي على جميع الرسائل

---

## 🔄 ربط النظام مع Backend (للمستقبل)

### الخيار 1: API Endpoint

**الملف:** `js/components/feedback.js`  
**السطر:** 287

```javascript
const API_ENDPOINT = '/api/feedback'; // غيّر هذا لـ API الخاص بك
```

**مثال Backend (PHP):**
```php
<?php
// api/feedback.php
header('Content-Type: application/json');

$data = json_decode(file_get_contents('php://input'), true);

// حفظ في قاعدة البيانات أو ملف
file_put_contents('feedbacks.json', json_encode($data) . "\n", FILE_APPEND);

echo json_encode(['success' => true]);
?>
```

---

### الخيار 2: إرسال Email

**يمكن إضافة:**
```javascript
async sendToEmail(data) {
    // إرسال عبر EmailJS أو Backend
    const emailData = {
        to: 'your-email@example.com',
        subject: `اقتراح جديد: ${data.type}`,
        body: `
            النوع: ${data.type}
            الاسم: ${data.name}
            البريد: ${data.email}
            الصفحة: ${data.page}
            الرسالة: ${data.message}
        `
    };
    // إرسال...
}
```

---

### الخيار 3: قاعدة بيانات

**مثال MySQL:**
```sql
CREATE TABLE feedbacks (
    id INT AUTO_INCREMENT PRIMARY KEY,
    type VARCHAR(50),
    name VARCHAR(100),
    email VARCHAR(100),
    page VARCHAR(255),
    message TEXT,
    timestamp DATETIME,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 📊 بيانات الرسالة

كل رسالة تحتوي على:

```json
{
    "type": "suggestion",
    "name": "أحمد محمد",
    "email": "ahmed@example.com",
    "page": "index.html",
    "message": "اقتراحي هو...",
    "timestamp": "2025-01-XX...",
    "userAgent": "Mozilla/5.0...",
    "url": "http://127.0.0.1:5500/index.html"
}
```

---

## 🎯 الحلول المقترحة

### الحل السريع (الآن):
1. ✅ استخدم `feedback-admin.html` لرؤية الرسائل
2. ✅ صدّر JSON بانتظام
3. ✅ احفظ ملفات JSON في مكان آمن

### الحل المتوسط (قريباً):
1. 🔄 ربط مع Email (EmailJS)
2. 🔄 إرسال لـ WhatsApp Business API
3. 🔄 حفظ في ملف JSON على السيرفر

### الحل الكامل (للمستقبل):
1. 🔄 Backend API (PHP/Node.js)
2. 🔄 قاعدة بيانات (MySQL/MongoDB)
3. 🔄 Dashboard كامل
4. 🔄 إشعارات Email

---

## 📝 خطوات الإعداد السريع

### 1. رؤية الرسائل الآن:
```
افتح: feedback-admin.html
```

### 2. تصدير الرسائل:
```
اضغط: "تصدير JSON" في feedback-admin.html
```

### 3. ربط مع Email (اختياري):
- استخدم EmailJS (مجاني)
- أو Backend لإرسال Email

### 4. ربط مع Backend (للمستقبل):
- غيّر `API_ENDPOINT` في `js/components/feedback.js`
- أنشئ API endpoint
- اختبر الإرسال

---

## ⚠️ ملاحظات مهمة

1. **LocalStorage محدود:**
   - سعة: ~5-10MB حسب المتصفح
   - يمكن مسحها بمسح بيانات المتصفح
   - كل مستخدم يرى رسائله فقط

2. **للإنتاج:**
   - يجب ربط النظام مع Backend
   - حفظ في قاعدة بيانات
   - إشعارات Email

3. **الأمان:**
   - تحقق من البيانات قبل الحفظ
   - منع XSS attacks
   - Rate limiting

---

## 🔗 روابط مفيدة

- **صفحة Admin:** `feedback-admin.html`
- **الكود:** `js/components/feedback.js`
- **API Endpoint:** السطر 287

---

**آخر تحديث:** 2025

